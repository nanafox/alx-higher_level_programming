-- This script displays the number of records with `id = 89` in the
-- 'first_table' of the database 'hbtn_0c_0'

-- display the number of records whose `id` is equal to `89`
SELECT COUNT(id) FROM first_table WHERE id = 89
