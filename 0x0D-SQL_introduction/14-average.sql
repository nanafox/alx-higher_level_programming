-- This script computes the the score average of all records in the
-- table 'second_table' of the database 'hbtn_0c_c'

-- compute the average of the scores
SELECT AVG(score) AS average
FROM second_table;
